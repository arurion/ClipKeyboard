# Changelog

このプロジェクトの変更点は [Keep a Changelog](https://keepachangelog.com/ja/1.0.0/)
の形式に沿って記録する。

## [1.1.3] - 2026-09-12

UI仕様書「Tinted Glass UI Philosophy」の指示に厳格に従い、以下を全面的に修正した。

### 修正
- **IMEサービス内での `BottomSheetDialog` 使用によるクラッシュを根絶。**
  `InputMethodService` はWindow Tokenを持たないため、ダイアログ表示は
  `BadTokenException` を誘発しうる。キーボードルートの `FrameLayout` に
  Viewレベルの「クイックアクション・オーバーレイ」を組み込み、ダイアログを
  一切使わずに長押しメニュー(ピン留め/編集/削除)を表示するよう変更した。
- **入力キーを持たないIME内にあった検索用`EditText`を撤廃。**
  文字入力ができないコピペIMEの中に検索窓を置くのはアンチパターンと判断し、
  ヘッダーを「IME切替」「クリップボード(件数)」「定型文作成」
  「キーボードを閉じる」の4要素、すべて44dp以上のタッチターゲットに整理した。
- **メイン画面に「① キーボード有効化」「② キーボード切替」の常設コントロールを復活。**
  以前のUI刷新で撤去していたが、道具としての基本動線として画面上部に固定し、
  有効化済みかどうかをボタン表示自体が示すようにした。
  一覧アイテムのタップは「クリップボードへコピー＆トースト表示」、
  長押しは「編集画面へ遷移」に統一。
- **ダークモード対応をやり直し(1.1.2の「ダークモード無効化」対応から方針転換)。**
  `values-night/themes.xml` を復活させ、ライトはサックスホワイト+Skyブルー、
  ダークはSlate基調で統一。加えて、すべての `ImageButton` に
  `app:tint` を明示指定し、アイコンが背景色と同化して見えなくなる問題を解消した。
- **`ThemeUtils.glassRowBackground` の `setLayerInset` インデックス指定ミスを修正。**
  影レイヤーではなく本体(ガラス面)レイヤーの方を誤って削っていたため、行の
  見た目が欠けて見える不具合があった。影(インデックス0)側を正しくオフセットする
  よう修正し、`RippleDrawable` のマスクも単一の角丸シェイプに分離して境界漏れを防いだ。
- **テーマ設定画面の初期化バグとプリセットのはみ出しを修正。**
  `bindFields()` 実行中に各カラー欄の `TextWatcher` が反応し、空文字状態で
  プレビュー再描画が連打される不具合を `isBinding` フラグで抑止。
  プリセットボタン群を `HorizontalScrollView` で囲み、種類が増えても
  画面外にはみ出さないようにした。

### 変更
- クリップのタップ操作を「ペースト目的でのコミット」から
  「システムクリップボードへコピー(`ClipboardManager#setPrimaryClip`)+トースト表示」
  に変更(メイン画面側)。IME側は従来通りタップ即コミット。

## [1.1.2] - 2026-09-12

### 修正
- **ダークモードで配色がちぐはぐになり見づらい問題を修正。**
  アプリのテーマが `Theme.MaterialComponents.DayNight` だったため、
  システムをダークモードにすると `EditText`・`Switch`・ボタンなどの
  ネイティブ部品だけが自動的にダーク配色になり、自前で描画している
  ガラス調の行・ヘッダー(常にライトパレット固定)と衝突してひどく
  読みにくくなっていた。
  Tinted Glass UI Philosophy の「基本はライトテーマ、ダーク系を
  独りよがりなデフォルトにしない」という原則に立ち返り、アプリの
  ベーステーマを `Theme.MaterialComponents.Light.NoActionBar` に固定し、
  `values-night` のテーマ上書きを削除した。ダーク配色が欲しい場合は
  OSのシステム設定ではなく、アプリ内の「テーマ編集」から「スレート」
  プリセットを選ぶ形に統一する。
- メイン画面・クリップ編集画面にも保存済みテーマ(背景色・文字色・
  アイコン色・FABの色)を適用するようにし、IMEビューだけでなく
  アプリ全体で配色が一貫するようにした。
- クリップ行のガラス面の不透明度を alpha 200/255(約0.78、ほぼ不透明な
  パネル)から 140/255(約0.55)に下げ、仕様書の透明度基準
  (alpha 0.3〜0.5、0.6超は不透明パネル化を疑う)に近づけた。

## [1.1.1] - 2026-09-12

### 修正
- **起動時に強制終了する重大な不具合を修正。**
  `MainActivity#onResume` が `Settings.Secure.ENABLED_INPUT_METHODS` /
  `DEFAULT_INPUT_METHOD` を直接読んでいたが、Android 14 (API 34) 以降は
  これらのキーが `targetSdkVersion` 34 以上のアプリからは
  `SecurityException` を投げるようになっており、アプリ起動直後に必ず
  クラッシュしていた。制限のない公式API
  `InputMethodManager#getEnabledInputMethodList()` を使う実装に置き換えた。
  なお、この変更に伴い「キーボードは有効だが選択されていない」状態を
  区別する案内は行わず、「有効かどうか」のみでバナー表示を判定するよう
  簡略化した。

## [1.1.0] - 2026-09-12

### 追加
- UI/UX を「Tinted Glass UI Philosophy」設計書に基づき全面的に作り直した。
  - IMEビューの高さを 244dp に完全固定し、クリップ件数の増減で周囲のレイアウトが
    伸縮しない構造にした(`res/layout/ime_keyboard_view.xml`)。
  - ヘッダーに「IME切替」「絞り込み検索」「件数表示」「定型文の新規作成」
    「キーボードを閉じる」を1行に集約。
  - 絵文字(📌/🗑)を廃止し、ベクタードローアブル + テーマ色のTintに置き換えた。
  - クリップの行(Row)を、単色のベタ塗りではなく「淡いシルバーグレーの極細エッジ」
    +「下方向へのごく微細なシャドウ」を重ねたガラス調の背景に変更
    (`ThemeUtils.glassRowBackground`)。色や角丸はすべてテーマ値から動的生成するため、
    どの配色プリセットでも同じ質感原則が保たれる。
  - 長押しで「ピン留め切替 / 編集 / 削除」を選べるボトムシート形式のクイックメニューを追加。
    誤操作しやすい削除を行の表面から隠蔽した。
  - 直前のキーボードへ戻る操作を、Android 9(API 28)以降の
    `switchToPreviousInputMethod()` → 失敗時 `switchToNextInputMethod(false)` →
    それも失敗した場合は `showInputMethodPicker()` という3段のフォールバックに強化。
  - メイン画面を再設計。ベタ塗りボタンの羅列をやめ、IME未有効時のみ表示される
    セットアップ案内バナー、右下のFAB(新規作成)、オプションメニュー配下に隔離した
    「全消去」(二重確認ダイアログ付き)を導入。
  - クリップ編集画面に新規作成モードを追加(`EXTRA_CLIP_ID` 未指定で新規作成として動作)。
    保存ボタンをツールバー右上に固定し、スクロールやキーボード表示で押せなくなる問題を解消。
    入力欄を `TextInputLayout`(OutlinedBox)に変更。
  - テーマ設定画面に、変更が即時反映されるミニキーボードのライブプレビュー、
    現在の色を示すカラーチップ、スライダーの現在値表示を追加。
    プリセットを「デフォルト・ガラス」「スレート」「サクラ」の3種に更新。
  - デフォルトの配色をサックスホワイト基調の「Tinted Glass」パレットに変更
    (背景 `#EAF5FF` / 行 `#F8FAFC` / 文字 `#0F172A` / アクセント `#0369A1` など)。
  - `MainActivity#onResume` でテーマとデータを同時に再読込し、テーマ設定画面から
    戻った際に一覧の見た目が更新されない不具合を修正。
- `ClipStore.createManual()` を追加。手動での定型文作成(FAB・キーボードの「＋」・
  ファイルインポート)を、クリップボード監視による自動統合(`addOrTouch`)と
  区別して扱えるようにした。

### 変更
- `androidx.coordinatorlayout` を依存関係に追加(FABの配置に使用)。

## [0.1.0] - 初期リリース

- クリップボードの自動監視・履歴保存(ピン留め・検索・削除)。
- タップでの即時ペースト、長押しでの編集画面呼び出し。
- テキストファイルのインポート(1行ずつ複数クリップ化 / 全文1件を選択可能)。
- 配色などのテーマ編集(16進コード直接入力)。
- パッケージ名を `dev.arurion.clipkeyboard` から `dev.clipkeyboard` に変更。
- GitHub Actions によるビルド/リリースワークフローを追加。
  リリース署名鍵が未設定の場合は自動でデバッグ署名にフォールバックする仕組みを実装。
- タップ時のハプティックフィードバック、フルスクリーン抽出モードの無効化に対応。
- ライセンスを Apache License 2.0 に統一。
